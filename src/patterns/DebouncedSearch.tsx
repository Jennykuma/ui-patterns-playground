import Card from '../components/Card';

const DebouncedSearch = () => {
  const content = <div>huh</div>;

  return (
    <Card patternName="Debounced Search" description="Temp description here">
      {content}
    </Card>
  );
};

export default DebouncedSearch;
