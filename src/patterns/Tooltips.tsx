import Card from '../components/Card';

const Tooltips = () => {
	return (
		<Card
			patternName="Tooltips"
			description="A tooltip is a small pop-up box that appears when a user hovers over an element, providing additional information about that element. Tooltips are commonly used to provide context or explanations for icons, buttons, or other interactive elements on a webpage."
		>
			WIP
		</Card>
	);
};

export default Tooltips;
