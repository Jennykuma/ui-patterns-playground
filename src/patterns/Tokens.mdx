import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';

<Meta title="Design Tokens/Colors" />

# Colors

<ColorPalette>
  <ColorItem
    title="primary"
    subtitle="Buttons, main actions"
    colors={{ default: '#5a6e4a', hover: '#4a5d3a' }}
  />
  <ColorItem
    title="secondary"
    subtitle="Buttons, secondary actions"
    colors={{ 'border/text': '#5a6e4a', 'hover bg': '#eef1ea' }}
  />
  <ColorItem
    title="danger"
    subtitle="Delete, remove actions"
    colors={{ default: '#963d32', hover: '#7d3229' }}
  />
  <ColorItem
    title="sage-green"
    subtitle="Badges, tinted backgrounds"
    colors={{ default: '#9caf88' }}
  />
  <ColorItem
    title="pale-pink"
    subtitle="Accents"
    colors={{ default: '#ffc0c0' }}
  />
</ColorPalette>

`focus` aliases `primary` and is used for focus outlines and rings (e.g. `outline-focus`, `ring-focus`).
