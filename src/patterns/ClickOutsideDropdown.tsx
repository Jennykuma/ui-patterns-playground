import Card from '../components/Card';

const ClickOutsideDropdown = () => {
  const content = <div></div>;

  return (
    <Card patternName="Click-Outside Dropdown" description="temp description">
      {content}
    </Card>
  );
};

export default ClickOutsideDropdown;
